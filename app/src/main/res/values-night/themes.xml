<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sa3id" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">#FFFFFF</item> <!-- White background -->
<!--        <color name="chip_text_color">#FFFFFF</color>-->
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- White status bar -->
        <item name="android:windowLightStatusBar">true</item> <!-- Light text -->
        <item name="colorDynamic">@color/dark_background</item>
        <item name="colorDynamicAlpha">@color/dark_background_alpha</item>
        <item name="colorDynamicFlipped">@color/light_background</item>
        <item name="colorDynamicAlphaFlipped">@color/light_background_alpha</item>
    </style>
</resources>